{$PUBLIC_DOMAIN:falconmedai.uz} {
    @api path /api/* /metrics /healthz /readyz /livez /api-docs
    @tma path /tma.html /scribe.html /dashboard.html /admin-tma.html /qr-pay.html /qr-pay-kiosk.html /face_id.html /face-kiosk.html /mini-app.html /referral_portal.html /face-capture.js /face-capture.css /models/*
    handle @api {
        reverse_proxy {$APP_URL:http://app:3000}
    }
    handle @tma {
        reverse_proxy {$APP_URL:http://app:3000}
    }
    handle {
        reverse_proxy {$FRONTEND_URL:http://frontend:3001}
    }
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "camera=*, microphone=*"
        -Server
    }
}

http://{$SERVER_IP:167.233.44.193} {
    @api path /api/* /metrics /healthz /readyz /livez /api-docs
    @tma path /tma.html /scribe.html /dashboard.html /admin-tma.html /qr-pay.html /qr-pay-kiosk.html /face_id.html /face-kiosk.html /mini-app.html /referral_portal.html /face-capture.js /face-capture.css /models/*
    handle @api {
        reverse_proxy {$APP_URL:http://app:3000}
    }
    handle @tma {
        reverse_proxy {$APP_URL:http://app:3000}
    }
    handle {
        reverse_proxy {$FRONTEND_URL:http://frontend:3001}
    }
}
